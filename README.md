# Создаем таблицу всех регионов

## Требуется python версии 3+

## Запуск

Устанавливаем требуемые библиотеки:

```zsh
pip install -U -r requirements.txt
```

Кладем рядом табличку от разработчиков, например "EquinoxSystemResourceData4.xlsx"

Запускаем:

```zsh
python main.py
```
